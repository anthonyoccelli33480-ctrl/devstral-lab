# Contributing

1. Register agent in `backend/app/agents/registry.py`
2. Add guide in `backend/app/agents/guides.py`
3. Pick `devstral-small-latest` or `codestral-latest`
4. JSON-only system prompt
5. PR → CI must pass